#Title = "Capture2Net"
#InternName = #Title; Must be a string without special characters (Used for file names, registry values, ...)!

Global eGlobals.eGlobals
Global NewList ConfigGadgets.eConfigGadgets()
Global NewList ConfigValues.eConfigValues()
Global NewList UploadQueue.eUploadQueue()
; IDE Options = PureBasic 4.60 (Windows - x86)
; EnableXP
; EnableCompileCount = 0
; EnableBuildCount = 0
; EnableExeConstant